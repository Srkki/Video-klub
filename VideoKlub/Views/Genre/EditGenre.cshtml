@model Genre

    <form asp-controller="genre" asp-action="SaveEditGenre" method="POST" class="my-1">
        <div class="row mb-1 justify-content-md-center">
            <div class="col-1">
                <label asp-for="@Model.GenreName"></label>
            </div>
            <div class="col-4">
                <input asp-for="@Model.GenreName" type="text" class="form-control" placeholder="Enter genre" />
                <span asp-validation-for="@Model.GenreName" class="text-danger"></span>
            </div>
        </div>
        <div class="row mb-1 justify-content-md-center">
            <div class="col-1">
                <label asp-for="@Model.GenreColor"></label>
            </div>
            <div class="col-4">
                <input asp-for="@Model.GenreColor" type="color" value="@Model.GenreColor" />
            </div>
            <input asp-for="@Model.GenreId" type="hidden" value="@Model.GenreId" />
        </div>
        <div class="mb-1 justify-content-md-center">
            <input type="submit" class="btn btn-info custom_btn" value="Save" />
            <a asp-controller="movie" asp-action="create" class="btn btn-primary custom_btn m-2">Cancel</a>
        </div>
    </form>